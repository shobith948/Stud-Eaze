<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.studeaze">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudEaze">
        <activity android:name=".AttendanceAdapter"/>
        <activity android:name=".MarksAdapter" />
        <activity android:name=".AddMarksActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".TimeTableActivity" />
        <activity android:name=".ImageShowActivity" />
        <activity android:name=".NoticeAdapter" />
        <activity android:name=".LoadingDialogAcitivity" />
        <activity android:name=".AddNoticeActivity" />
        <activity android:name=".TeacherDashboardActivity" />
        <activity android:name=".AddTeacherActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".StudentRegisterActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".TeacherLoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>